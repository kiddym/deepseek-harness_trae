# 任务管理 Web 应用交付策略

## 1. 交付目标

用一条短而完整的路径交付第一期可验证产品：先冻结需求与设计，再建立最小运行骨架，按从持久化到 API、前端、端到端测试、CI 的顺序集成，最后用验收条件和 CI 证据收口。

## 2. 交付路径

### 阶段一：文档基线

产物是 PRD、设计、交付策略和项目计划。检查范围、非目标、接口契约、数据结构、假设与验收条件是否一致。

### 阶段二：运行骨架

建立 React + TypeScript + Vite 前端、Node.js + TypeScript + Fastify 服务、TypeScript 配置、统一脚本和基础环境配置。此阶段只追求能启动，不扩展产品功能。

### 阶段三：数据层与 API

先实现 SQLite 初始化和任务仓储，再实现任务列表、创建、编辑、完成切换、删除接口。用接口级检查确认校验、错误码、持久化和重启保留行为。

### 阶段四：前端主流程

接入页面加载、创建、编辑、完成切换和删除。每完成一个动作，使用服务端返回对象更新状态，并覆盖加载、失败和重复提交状态。

### 阶段五：端到端验证

用 Playwright Test 固化 PRD 中的最小用户旅程，重点验证刷新和服务重启后的持久化，而不是只验证内存中的 UI 变化。

### 阶段六：CI 与交付收口

配置 GitHub Actions Linux 工作流，执行安装、构建、服务启动和 Playwright Test，保存失败诊断产物。最后逐项对照验收条件，记录未完成项和剩余风险。

## 3. 关键假设

- 第一阶段只支持单用户本地使用，不引入身份系统或权限模型。
- SQLite 文件是唯一持久化来源；浏览器 localStorage 不承担任务数据存储。
- Fastify 是唯一后端 HTTP 服务，不拆分服务。
- 前后端通过 JSON HTTP API 通信，不使用 SSR 或外部 API。
- Node 运行环境支持 `node:sqlite`；实现阶段需要确认目标 Node 版本满足该要求。
- Playwright Chromium 是第一期浏览器基线，暂不承诺所有浏览器。
- 默认使用 `npm` 和 GitHub Actions 官方 Linux runner；除非仓库现状要求，否则不引入额外包管理器。
- 任务数据无需迁移历史版本，数据库 schema 以第一期初始化为主。

## 4. 集成顺序与质量门

每个阶段完成后都保留一个可观察结果：

1. 文档基线：接口和验收条件没有未决冲突。
2. 运行骨架：前后端可分别启动，TypeScript 构建通过。
3. 数据与 API：接口返回契约稳定，服务重启后数据仍在。
4. 前端：所有核心操作可从页面完成，错误不会静默丢失输入。
5. E2E：核心用户旅程在干净数据库上通过。
6. CI：Linux 环境从干净检出开始重复得到相同验证结果。

质量门未通过时，暂停向后集成，先修复当前层的问题，避免把环境问题伪装成产品功能问题。

## 5. 重评触发

出现以下情况时重新评估范围、假设或顺序，并更新 PROJECT-PLAN.md：

- `node:sqlite` 与目标 Node 版本或 CI runner 不兼容。
- 单体服务同时提供静态资源和 API 的方案引入超出第一期的复杂度。
- Playwright 测试需要非本地依赖、外部 API 或不可控服务。
- 数据模型需要标签、截止日期、用户隔离等新概念。
- 需求新增登录、多用户、权限、部署或其他已声明非目标。
- 刷新或服务重启后数据丢失，且原因不属于测试隔离配置。
- API 契约变更导致已实现前端或测试需要返工。
- CI 时间、稳定性或诊断能力不足以支持可靠交付。

重评时优先保持第一期核心闭环，明确记录取舍，不通过隐式扩展范围解决新问题。
