# 任务管理 Web 应用 PRD

## 1. 产品目标

这是一个最小可用的单用户任务管理 Web 应用，用于走通从需求、设计、实现、测试到 CI 的完整交付流程。用户可以创建任务、查看任务列表、编辑并保存任务、刷新页面后保留数据、标记任务完成，以及删除任务。

## 2. 用户与使用场景

### 2.1 用户

第一期面向本地开发环境中的单一使用者。不区分账号、角色或权限。

### 2.2 核心场景

1. 用户打开应用，看到当前保存的任务列表。
2. 用户输入任务标题，可选填写描述，创建一条未完成任务。
3. 用户修改任务标题或描述并保存，列表展示最新内容。
4. 用户刷新浏览器，之前创建和修改的任务仍然存在。
5. 用户将任务标记为完成，也可以取消完成状态。
6. 用户删除任务，任务不再出现在列表中。

## 3. 第一期范围

- 单页面任务列表。
- 创建任务：标题必填，描述可选。
- 查看任务：按更新时间倒序展示。
- 编辑任务并保存。
- 切换完成/未完成状态。
- 删除任务，并要求用户确认。
- 使用 SQLite 持久化任务数据。
- React + TypeScript + Vite 前端。
- Node.js + TypeScript + Fastify 单体轻量 HTTP 服务。
- 使用 Node 内置 `node:sqlite`，不引入第三方 SQLite 驱动。
- Playwright Test 端到端验证。
- GitHub Actions Linux CI。

## 4. 非目标

第一期明确不做：登录、多用户、权限、文件上传、外部 API、服务端渲染、微服务、桌面安装包、收费与部署。

同时不做任务标签、优先级、截止日期、排序筛选、搜索、批量操作、提醒通知、附件、评论、历史版本和离线同步。

## 5. 行为预期

- 标题为空或仅包含空白字符时，不能创建或保存任务，并显示明确提示。
- 创建成功后，表单清空，新增任务立即出现在列表中。
- 新任务初始为未完成。
- 编辑状态下保存成功，退出编辑状态并展示最新内容。
- 编辑失败时保留用户输入，并显示错误，不静默丢失内容。
- 切换完成状态成功后，任务的完成视觉状态与服务端数据一致。
- 删除前显示确认；取消确认则任务保持不变。
- 删除成功后，任务从列表移除。
- 首次加载、保存、切换和删除期间应有明确的加载或禁用状态，避免重复提交。
- 网络或服务端错误应显示可理解的错误信息，并允许用户重试。
- 刷新页面后，数据从 SQLite 重新加载，而不是依赖浏览器内存或 localStorage。

## 6. 验收条件

以下条件全部满足，视为第一期功能验收通过：

- 可以在页面创建标题合法的任务。
- 创建后的任务在列表中可见，且初始为未完成。
- 可以编辑标题和描述并保存，重新加载列表后内容正确。
- 可以将任务标记为完成，再次刷新后仍为完成状态；也可以恢复为未完成。
- 可以删除任务，确认删除后刷新页面仍不会重新出现。
- 空标题不能被创建或保存，并有可见提示。
- 服务重新启动后，已保存任务仍存在。
- 前端通过 HTTP 接口与 Fastify 服务通信，数据由 SQLite 持久化。
- Playwright Test 覆盖创建、刷新保留、编辑、完成切换和删除主流程。
- GitHub Actions 在 Linux 环境执行安装、构建和 Playwright 测试，并在失败时提供可诊断结果。

## 7. 采用的假设

- 默认服务运行在本机，前端开发服务器与 API 服务使用固定的本地开发地址；具体端口由 DESIGN.md 约定。
- 任务只包含标题、描述、完成状态、创建时间和更新时间。
- 任务 ID 使用服务端生成的字符串或整数，客户端不生成持久化 ID。
- 任务列表按 `updated_at` 倒序，同一时间的顺序以 ID 作为稳定次序。
- 时间由服务端生成并以 ISO 8601 字符串返回。
- 删除是硬删除；第一期不保留回收站或撤销记录。
- 由于没有登录，多标签页并发编辑不作为保证场景；最后一次成功保存覆盖之前内容。
- 浏览器支持范围以 Playwright 默认 Chromium 为第一期基线。
