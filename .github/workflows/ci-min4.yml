name: ci-min4
on:
  push:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      PLAYWRIGHT_BROWSERS_PATH: ${{ runner.temp }}/pw-browsers
    steps:
      - run: echo "browsers=$PLAYWRIGHT_BROWSERS_PATH"
